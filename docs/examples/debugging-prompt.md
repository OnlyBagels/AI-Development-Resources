# Debugging Prompt

```md
Task: Debug the failing test below.
Context: This started after refactoring the auth middleware. Relevant files are likely `src/middleware/auth.ts` and `src/routes/session.ts`.
Goal: Identify root cause and propose the smallest fix.
Constraints: Do not rewrite the auth system. Do not change public API behavior.
Return:
1. Root cause
2. Minimal patch plan
3. Tests to run
4. Risks

Failure:
<paste only relevant failure excerpt>
```
