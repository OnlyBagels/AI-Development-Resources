# Hooks

Hooks can reduce token waste by preprocessing noisy tool output before Claude reads it.

This folder includes one example: `filter-test-output.sh`.

Use hooks carefully. They run commands automatically and should be easy to inspect.
